{
  "name": "shared-recources",
  "version": "1.0.0",
  "author": "Philipp Horwath",
  "license": "MIT",
  "description": "a shared recources project for wall spark engine examples",
  "scripts": {
    "build": "rm tsconfig.tsbuildinfo && rm -rf dist && tsc && tsc-alias"
  },
  "dependencies": {
    "@thespielplatz/wall-spark-engine": "^1.1.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsc-alias": "^1.8.11",
    "typescript": "^5.8.2"
  }
}
